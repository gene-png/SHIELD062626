# Project Context — state of `main`

_Last updated: 2026-07-08 (Sprint 2 close + dependabot policy). This file
describes the project as of the branch it sits on and is updated ONLY as part
of a PR. Durable facts and environment gotchas live in `CLAUDE.md`; personal
in-flight status lives in
`context/<name>.md`; per-sprint detail lives in `SPRINT_<n>.md`._

## Current state

- **v2 work order (Parts A–F) merged to `main`** (PR #1, migrations 0015–0025,
  `v3.0.0`): all four service surfaces, multi-tenant onboarding, AI job
  registry, CSF Playbook engine, Risk Register, F hardening pass.
- **Sprint 1 "smoke sweep" complete** (`qa/smoke-sweep-sprint-1`, PR #16):
  `SMOKE_TEST.md` backed by a green 14-file / 27-test Playwright suite under
  `e2e/smoke/` (sections 0–15), plus the runtime defects the sweep surfaced.
  Notable: offline fixture-mode AI (D-017), ATT&CK coverage PATCH proxy,
  typed registration errors (D-016).
- **Sprint 2 "findings burn-down" complete and MERGED (PR #19, 2026-07-08)** —
  `fix/findings-burndown-sprint-2`. All 11 tasks done with commits; full exit
  gate set green (16-file / 32-test e2e suite, `pytest -m unit`, web `tsc`);
  the new CI `e2e` job passed its first real runner execution on the PR.
  The suite is now 16 files / 32 tests (was 14/27 at Sprint 1 close: +s2
  management, +s16 axe).
- **Dependabot triaged under the D-018 policy (this PR):** activated by the
  PR #16 merge, it filed 15 backlog PRs. npm majors are now suppressed
  (`.github/dependabot.yml` ignore + weekly minor/patch groups); the 7 major
  PRs (react 19, next 16, tailwind 4, eslint 10, …) closed unmerged — the
  framework-major bundle is deferred to Sprint 3/4 now that e2e runs in CI;
  the 5 Actions bumps merged; autoprefixer/next-auth/prettier (#7/#13/#14)
  rebase-and-merge after this PR and PR #19 (lockfile ordering).

### Sprint 2 task → commit

| Task | What shipped | Commit |
| --- | --- | --- |
| T1 | Runtime e2e id resolution (`e2e/helpers/ids.ts`); 8 specs de-brittled | `1e6640a` |
| T0 | `next` → 14.2.35 dep bump; 0 crit / 5 high (15.x-only, documented) | `f580a3b` |
| T2 | Fresh-stack proof + `e2e/README.md` canonical bring-up | `64d5b95` |
| T3 | CI `e2e` job in `ci.yml` (composed stack + Playwright + artifacts) | `f0475ce` |
| T4 | Runtime axe sweep `s16` + `--ink-tertiary` contrast fix | `7603799` |
| T5 | IG Core/Supporting metadata → CSF Playbook Rules 2/5 fire | `ec59f1d` |
| T6 | Roving tabindex on tier/stage pickers + heatmap `scope="row"` | `137727b` |
| T7 | CSF draft-exists guard (idempotent 200, no unbounded minting) | `efa87b8` |
| T8 | `/admin/management` UI spec (s2) + verbatim CSF prompts (s3, C8) | `9dbb83f` |
| T9 | Reject reserved/special-use TLDs at domain approval (D-019) | `1cdfa89` |
| T10 | Docs refresh (BUILD_REPORT, CHANGELOG, SMOKE_TEST, CONTEXT) + D-018→D-019 | this commit |

## Machine-local facts (this box)

- **Web runs on port 3001**, not 3000: `.env` sets `WEB_PORT=3001` /
  `NEXTAUTH_URL=:3001` because a separate `kentro-cloud-modernization` next-dev
  holds `:3000`. Playwright resolves the port via `e2e/helpers/baseUrl.ts` —
  never hardcode `:3000` in new specs; use the helper or a relative baseURL.
  Canonical/CI stays `:3000`. Committed parametrization: `a26e6cd`.
- **gh CLI has two accounts** (2026-07-08): active `SpearheadAnalytica` (full
  write) and `david-catarious_kentro` (Kentro EMU — GitHub blocks EMU writes
  outside its enterprise; reads only). `gh auth switch --user <name>` to flip.
  `git push` authenticates as SpearheadAnalytica via GCM regardless.
- **Tooling not on default PATH:** `node.exe` + `gh.exe` live under
  `%LOCALAPPDATA%\Microsoft\WinGet\Packages`. Run e2e via that `node.exe` +
  `e2e/node_modules/@playwright/test/cli.js`. Docker CLI needs
  `export PATH="$PATH:/c/Program Files/Docker/Docker/resources/bin"` per shell.

## Deferred / needs a human

- **Merge the remaining npm dependabot PRs** (#7 autoprefixer, #13 next-auth,
  #14 prettier) after this PR — each needs a `@dependabot rebase` onto the
  post-#19 lockfile; #14 additionally needs a reformat commit (prettier 3.9.4
  formats a few files differently than 3.8.3).
- **SMOKE_TEST §10:** eyeball the 8 export artifacts in `e2e/artifacts/`
  (regenerated by s7/s8; each already asserted 200 + content-type).
- **SMOKE_TEST §14:** one live-AI run; confirm a redacted `llm_calls` row, no
  PII.
- **Sprint 3 infra:** `infra/terraform` (cloud/account/region/network), MFA +
  email-verify flags, FedRAMP LLM connector — see `DELIVERY_PLAN.md`.
- **Backlog found-not-fixed:** the attack/zt/tech-debt assessment mint routes
  share CSF's old unbounded-version pattern — T7 guarded CSF only. Also a
  pre-existing duplicate `D-015` heading in DECISIONS.md (predates Sprint 2).

## Test coverage status

- Backend: full `pytest -m unit` green in-container (CSF/risk/ZT engines,
  cross-tenant isolation, runtime AI fixtures, IG-metadata Rules 2/5, CSF
  draft-guard, reserved-TLD guard).
- Web: `tsc --noEmit` clean.
- e2e: 32/32 green across 16 spec files (host, resolves `:3001`); includes the
  new `s16` axe WCAG A/AA sweep and `s2` management-UI spec. Known cold-compile
  flake under load documented in `CLAUDE.md` — a re-run clears it.

## Lessons learned (Sprint 2)

- **De-brittle before you re-seed.** The 8 hardcoded-UUID specs only surfaced as
  broken once the local image/volume cache was empty at launch and the DB
  reseeded with fresh ids — hence T0 was reordered to depend on T1. Runtime id
  resolution (`ids.ts`) is now the contract; a repo grep enforces zero
  hardcoded UUIDs under `e2e/`.
- **Reuse the framework's own validation, don't hand-roll.** The reserved-TLD
  guard (T9) reuses email-validator's `SPECIAL_USE_DOMAIN_NAMES` check via a
  probe — the exact check `EmailStr` runs at registration — so approval and
  registration can never drift.
- **Fix at the point of the mistake, fail loudly.** `beacon.test` was
  approvable-but-unregistrable; rejecting at approval time (not silently later
  at registration) is the fail-loud posture. Same spirit as the CSF draft guard
  returning the existing draft instead of minting silently.
- **a11y focus vs. auto-save.** Roving-tabindex that auto-selects on arrow would
  flood the auto-save PATCH; decoupling focus from selection (select on
  Space/Enter/click) keeps both the a11y contract and the data plane sane.
- **Verbatim source of record:** the CSF interview prompts are the
  `app/csf/catalog.py` `SUBCATEGORIES` (NIST CSF 2.0 Final text), NOT the master
  spec `.txt` (which defines only the subcategory model) — documented in the s3
  spec + SMOKE_TEST §3.
- **CI honesty:** the new e2e job's first real run is pending the
  review-required push; its step block was proven locally against
  `e2e/README.md`, and that limitation is stated in the commit body — not
  claimed as green.
