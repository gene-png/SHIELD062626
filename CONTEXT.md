# Project Context — state of `main`

_Last updated: 2026-07-06. This file describes the project as of the branch it
sits on and is updated ONLY as part of a PR. Durable facts and environment
gotchas live in `CLAUDE.md`; personal in-flight status lives in
`context/<name>.md`; per-sprint detail lives in `SPRINT_<n>.md`._

## Current state

- **v2 work order (Parts A–F) merged to `main`** (PR #1, migrations 0015–0025,
  v3.0.0): all four service surfaces, multi-tenant onboarding, AI job
  registry, CSF Playbook engine, Risk Register, F hardening pass.
- **Sprint 1 "smoke sweep" complete — PR #16 open** (`qa/smoke-sweep-sprint-1`,
  19 commits, final `108832e`): `SMOKE_TEST.md` is now backed by a green
  14-file / 27-test Playwright suite under `e2e/smoke/` (sections 0–15), plus
  the runtime defects the sweep surfaced, fixed. Full detail: `SPRINT_1.md`
  and the PR #16 description. Notable: fixture-mode AI is fully
  offline-exercisable (D-017); missing ATT&CK coverage PATCH proxy route
  added; typed registration errors (D-016).
- **Sprint 2 planned, not launched**: `SPRINT_2.md` + staged queue
  `.claude/sprint-queue.sprint-2.json` (11 tasks: next@14.2.15 CRITICAL dep
  bump, runtime e2e id resolution, CI e2e + axe jobs, CSF IG Core/Supporting
  metadata import, small-fix burn-down, docs refresh). Roadmap:
  `DELIVERY_PLAN.md`.

## Backlog (found in Sprint 1, scheduled for Sprint 2)

- `next@14.2.15` CRITICAL vuln + ~29 pnpm advisories (S2 T0).
- Hardcoded seeded UUIDs in 8 e2e specs — break on a re-seeded DB (S2 T1).
- e2e suite not in CI; no runtime axe (S2 T3/T4).
- CSF Rules 2/5 use safe-default IG metadata — never fire (S2 T5).
- `beacon.test` approved-but-unregistrable reserved TLD (S2 T9).
- Roving tabindex missing on TierPicker/ZtStagePicker; heatmap `scope=row`
  (S2 T6). Unlimited CSF draft versions (S2 T7). `/admin/management` UI and
  C8 verbatim prompts not spec-covered (S2 T8). BUILD_REPORT/CHANGELOG stale
  (S2 T10).

## Needs a human

- SMOKE_TEST §10: eyeball the 8 export artifacts in `e2e/artifacts/`
  (regenerated by s7/s8 specs; each already asserted 200 + content-type).
- SMOKE_TEST §14: one live-AI run; confirm a redacted `llm_calls` row, no PII.
- Review/merge PR #16, then the collab-docs PR stacked on it.
- Sprint 3 infra decisions (cloud/account/region/network) — see
  `DELIVERY_PLAN.md`.

## Test coverage status

- Backend: full `pytest -m unit` green in-container (includes CSF/risk
  engines, cross-tenant isolation, runtime AI fixtures).
- Web: `tsc --noEmit` clean.
- e2e: 27/27 green (16.7 min) at sprint-1 close; known cold-compile flake
  under load documented in `CLAUDE.md`.
